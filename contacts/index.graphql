type ContactsEntry {
  email: String
  name: String
  other: String
  phone: String
  relation: String
  student_id: Int
}

type Query {
  contactsQuery: [ContactsEntry]
    @rest(endpoint: "https://test-mom-api.herokuapp.com/contacts")
}
