type VaccinationsEntry {
  covid: String
  flu: String
  mmr: String
  polio: String
  student_id: Int
  tb: String
  tetanus: String
}

type Query {
  vaccinationsQuery: [VaccinationsEntry]
    @rest(endpoint: "https://test-mom-api.herokuapp.com/vaccinations")
}
